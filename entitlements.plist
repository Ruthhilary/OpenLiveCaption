<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Allow microphone access for audio capture -->
    <key>com.apple.security.device.audio-input</key>
    <true/>
    
    <!-- Allow unsigned executable memory (required for Python/PyInstaller) -->
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    
    <!-- Disable library validation (required for Python dynamic libraries) -->
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
    
    <!-- Allow DYLD environment variables (optional, for debugging) -->
    <!-- Uncomment if needed for development -->
    <!--
    <key>com.apple.security.cs.allow-dyld-environment-variables</key>
    <true/>
    -->
    
    <!-- Allow JIT compilation (optional, for performance) -->
    <!-- Uncomment if using JIT-compiled code -->
    <!--
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    -->
</dict>
</plist>
